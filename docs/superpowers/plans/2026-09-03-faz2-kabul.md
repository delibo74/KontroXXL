# Faz 2 — Kabul kriterleri durumu (spec §8)

> Tarih: 2026-09-03 · Sürüm: 2.2.0 · Dal: `agent/worker-naslcd-faz2`
> Kural: **sahte onay yok.** Bu makinede gerçekten koşturulamayan hiçbir kriter "geçti"
> diye işaretlenmedi; koşulamayanlar "Karaduman'a borçlu" olarak duruyor.

| # | Kriter | Durum | Kanıt / neden |
|---|---|---|---|
| 1 | `dotnet build KontroXXL.sln -c Release` sıfır uyarı-hata | ✅ **Geçti** | `0 Uyarı, 0 Hata` (2026-09-03). |
| 2 | `dotnet test` yeşil | ✅ **Geçti** | 179/179 (Faz 2 başında 167; A9/D1-D4 +1, VersionText +12). |
| 3 | Temiz makinede kurulum exe'si kurar, kısayol koyar, uygulama açılır | ⏳ **Karaduman'a borçlu** | Paket üretildi (`releases/KontroXXL-win-Setup.exe`, 2.2.0, 7.2 MB) ama **kurulum yapılmadı**. Gerçek kurulum çalışan eski instance'ın kapatılmasını gerektiriyor. |
| 4 | `%APPDATA%\KontroXXL\config.json` oluşur; kurulum dizinine yazma olmaz | 🟡 **Kod düzeyinde doğrulandı, kurulumla değil** | Tüm yazılabilir yollar `AppPaths` üzerinden (`SpecialFolder.ApplicationData`). `BaseDirectory` kodda yalnızca **okumak** için geçiyor: `icon.ico` (MainForm:272, Tray:330) ve eski `config.json` göçü (Tray:81). Yazma yok. |
| 5 | Eski `Release_v2/config.json` göç eder | 🟡 **Testle doğrulandı, kurulumla değil** | `ConfigMigratorTests` — hedef **yokken** kopyalar, varken dokunmaz (idempotent). Gerçek kurulum senaryosu borçlu. |
| 6 | `config.json` içinde API anahtarı düz metin geçmez | 🟡 **Testle doğrulandı, canlı dosyayla değil** | Kayıt yolu yalnızca `TruenasApiKeyProtected` (DPAPI) yazıyor; düz metin alan kaydedilmiyor. Bu makinede `%APPDATA%\KontroXXL\config.json` **henüz yok** (uygulama bu ağaçtan hiç çalışmadı), o yüzden gözle doğrulama borçlu. Task 7 Adım 1'deki sır taraması onaydan önce zaten zorunlu. |
| 7 | Ayarlar → "Arduino'yu Programla" firmware yükler | ⛔ **Yapılmadı — Task 6 blokajlı** | Karaduman'dan `.hex` bekleniyor. Ek olarak `.gitignore`'daki `*.hex` satırı planın "hex'i depoya koy" adımıyla çelişiyor; karar gerekiyor. |
| 8 | Kaldırma uygulamayı siler, `%APPDATA%\KontroXXL\` kalır | ⏳ **Karaduman'a borçlu** | Velopack varsayılanı kullanıcı verisini silmez, ama gerçek kaldırma denenmedi. |
| 9 | Sürüm tek: props = paket = "Hakkında" | ✅ **Geçti** | `Directory.Build.props` `<Version>2.2.0</Version>`; `publish.ps1` exe damgasını props ile karşılaştırıp uyuşmazsa hata veriyor (`KontroXXL_WinApp.exe -> 2.2.0`); `pack.ps1` aynı değeri `--packVersion` yapıyor (paket `KontroXXL-2.2.0-full.nupkg`); Ayarlar → "Hakkında" `VersionText` ile aynı damgayı okuyor. |

## Ayrıca doğrulanan (spec §9 riski)

`vpk pack` her pakette şunu yazdırıyor: **"Verified VelopackApp.Run() in
`System.Void KontroXXL_WinApp.Program::Main()`"** — kurulum hook'larının sırası
makine tarafından doğrulanmış durumda.

## Açık maddeler

- **Task 6** (firmware yükleme): `.hex` + `.gitignore` çelişkisi kararı.
- **Task 7** (sır taraması + GitHub deposu): iki onay kapısı; `UpdateFeedUrl` sabiti
  o zamana kadar boş, "Güncellemeleri Denetle" yalnızca bilgi mesajı veriyor.
- Kurulum paketi **imzasız** — SmartScreen uyarısı beklenen (spec §9'da kabul edilmiş).
